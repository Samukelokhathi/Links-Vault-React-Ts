import { Modal } from "../OverlayModal/Modal";
import formStyle from "./Form.module.css";

const Form = () => {
  return (
    <Modal>
      <div className={formStyle["form-container"]}>{}</div>
    </Modal>
  );
};

export default Form;
